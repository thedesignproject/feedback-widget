name: Publish

on:
  push:
    tags:
      - 'v*'

jobs:
  # Build a tarball exactly as consumers will receive it from npm, install it
  # into a throwaway React app on each supported React major, and build. Catches
  # exports / peerDependencies / types mistakes that an in-repo test suite
  # cannot — they only surface from outside the monorepo.
  consumer-smoke:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        react: ['18', '19']
    name: consumer-smoke (react ${{ matrix.react }})
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run build
      - name: Pack tarball
        id: pack
        run: |
          TARBALL=$(bun pm pack | tail -1)
          echo "path=$(pwd)/$TARBALL" >> "$GITHUB_OUTPUT"
      - name: Build consumer app against React ${{ matrix.react }}
        env:
          TARBALL: ${{ steps.pack.outputs.path }}
          REACT_MAJOR: ${{ matrix.react }}
        run: |
          set -euo pipefail
          CONSUMER=$(mktemp -d)
          cd "$CONSUMER"
          cat > package.json <<'JSON'
          {
            "name": "consumer-smoke",
            "private": true,
            "version": "0.0.0",
            "type": "module"
          }
          JSON
          cat > index.tsx <<'TSX'
          import { FeedbackWidget } from '@thedesignproject/feedback-widget'
          export default function App() {
            return <FeedbackWidget projectId="smoke" apiBase="https://x" />
          }
          TSX
          cat > tsconfig.json <<'JSON'
          {
            "compilerOptions": {
              "target": "ES2020",
              "module": "ESNext",
              "moduleResolution": "bundler",
              "jsx": "react-jsx",
              "strict": true,
              "skipLibCheck": true,
              "noEmit": true,
              "esModuleInterop": true,
              "types": []
            },
            "include": ["index.tsx"]
          }
          JSON
          bun add \
            "react@$REACT_MAJOR" \
            "react-dom@$REACT_MAJOR" \
            "@types/react@$REACT_MAJOR" \
            "@types/react-dom@$REACT_MAJOR" \
            typescript
          bun add "$TARBALL"
          bunx tsc --noEmit

  publish:
    needs: consumer-smoke
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      # setup-node is kept solely for the final `npm publish` step — Bun can
      # install and build, but `npm publish --provenance` with GitHub's OIDC
      # story is the reliable publish path.
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          registry-url: 'https://registry.npmjs.org'
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      - run: bun run test
      - run: bun run build
      - name: Verify tag matches package.json version
        env:
          GITHUB_REF: ${{ github.ref }}
        run: |
          TAG_VERSION="${GITHUB_REF#refs/tags/v}"
          PKG_VERSION=$(bun -p "require('./package.json').version")
          if [ "$TAG_VERSION" != "$PKG_VERSION" ]; then
            echo "::error::Tag v$TAG_VERSION does not match package.json version $PKG_VERSION"
            exit 1
          fi
      - run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
